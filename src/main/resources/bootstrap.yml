spring:
  application:
    name: vital-module
  profiles:
    active: dev
  config:
    import: configserver:http://localhost:8888
  cloud:
    vault:
      host: localhost
      port: 8200
      scheme: http
      authentication: TOKEN
      token: s.WIn8zZlAjsQdi4zk2HxqqXlD
      kv-version: 2

server:
  port: 9200
